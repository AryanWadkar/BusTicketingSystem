{
  "name": "buts",
  "version": "1.0.0",
  "description": "Node app for Bus Utility Ticketing System",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node --max-old-space-size=128 ./build/index.js",
    "clean": "rimraf ./node_modules", 
    "reinstall": "npm run clean && npm install", 
    "rebuild": "npm run clean && npm install && npm run build"
  },
  "author": "Aryan Wadkar",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "cron": "^2.3.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-json-validator-middleware": "^3.0.1",
    "flat-cache": "^3.0.4",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.3",
    "nodemailer": "^6.9.1",
    "redis": "^4.6.7",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "rimraf": "^4.4.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
